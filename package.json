{
  "name": "nat20-auth",
  "version": "0.0.1",
  "description": "This is the authenticaiton service for nat20 application",
  "main": "auth.ts",
  "scripts": {
    "deploy-dev": "serverless deploy --stage dev && aws apigateway create-deployment --rest-api-id bgzxwzn8k3 --stage-name dev",
    "deploy-prod": "serverless deploy --stage dev && aws apigateway create-deployment --rest-api-id bgzxwzn8k3 --stage-name prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "12jihan",
  "license": "ISC",
  "dependencies": {
    "aws-amplify": "^6.6.5"
  }
}